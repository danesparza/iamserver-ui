//  React
import React, { Component } from 'react';

class AddReview extends Component {  

    render() {

      return (
        <div>
          When adding a new user (but before the user is actually added), you can: 
            Review all changes to be made          
        </div>
      );
    }
}

export default AddReview;